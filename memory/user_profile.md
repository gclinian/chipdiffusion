---
name: User profile — chipdiffusion researcher
description: Graduate-level ML researcher doing diffusion-model fine-tuning experiments to verify and beat the ChipDiffusion paper (arxiv 2407.12282) on ISPD2005 benchmarks
type: user
---

- 研究生/研究人員，在做 chipdiffusion paper verification + 改進實驗（DDPO, AddLoss, supervised fine-tune 等）。
- 中文溝通，技術內容可中英混用。熟悉 PyTorch、diffusion models、policy gradient、ReFL-style direct gradient。
- 工作流程非常系統化：`docs/plan/ → 執行 → docs/report/ → docs/next/ → docs/plan/(N+1)`，每次實驗前都會先寫 plan 文件，後寫 report 對照計畫判定標準。
- 需要 session 間的完整連續性——會依賴 `CLAUDE.md`、`PROGRESS.md`、`docs/context.txt` 來 hand off 狀態給下個 session。
